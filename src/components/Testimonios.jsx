import React from "react";

export const Testimonios = () => {
  /* const {user, isAuthenticated, isLoading} = useAuth0(); */

  return (
        <div>
            Testimonios
        </div>
    );
};